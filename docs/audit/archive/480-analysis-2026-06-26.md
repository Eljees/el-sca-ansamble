# Плановый анализ 2026-06-26

> Автоматический запуск scheduled task `fixups`.
> Базовая линия: коммит `eec9673` (HEAD на входе, master) — «docs: audit 470».
> Предыдущий аудит: `docs/audit/470-analysis-2026-06-24.md`.
> Режим: **анализ + исправления** (P3 carry-forward из 470 без ручных операций).

---

## 1. Состояние на вход (верифицировано)

| Параметр | Значение | Статус |
|---|---|---|
| HEAD на входе | `eec9673` | — |
| Версия | **0.1.5** | ✅ |
| Тег | `v0.1.5` | ✅ |
| Тестов (468-базовая) | **868 passed**, 0 failed | ✅ |
| ruff check | ✅ clean | ✅ |
| `origin/master` | 2 коммита впереди (8286c62, eec9673) | ⚠️ push нужен |

---

## 2. Верификация carry-forward из 470

| ID | Описание | Статус |
|----|----------|--------|
| A_OBS-2 | `_health_summary` → `retry: RetryPolicy` | ✅ **закрыт** (6202c1f) |
| STALE-COMMENT | `versions.env:10–11` — устаревший текст | ✅ **закрыт** (6202c1f) |
| GITLAB-CI-SYNC | `lint-versions` в `.gitlab-ci.yml` | ✅ **закрыт** (6202c1f) |
| AUDIT-ARCHIVE | Архив 310–390 (9 файлов) | ✅ **закрыт** (6202c1f) |
| PUSH | Аудит 460+470 на origin | ❌ **нужен push** (включая новый 6202c1f) |
| D-GITLAB | Force-push GitLab protected branch | ❌ ручной |
| B9 | NVD API key rotation | ❌ ручной (NEVER automate) |

---

## 3. Что сделано в этой сессии (коммит `6202c1f`)

### A_OBS-2 — `_health_summary` рефакторинг

**Файл:** `resilient_updates/cli.py`

Сигнатура изменена с 4 отдельных retry-параметров на единый `retry: RetryPolicy`:

```python
# было
def _health_summary(config, tool, layer, timeout,
    retry_count, backoff_seconds, retry_codes,
    non_retryable_reasons=None, session=None)

# стало
def _health_summary(config, tool, layer, timeout,
    retry: RetryPolicy, session=None)
```

Два call-site обновлены:
- **trivy**: теперь передаёт `retry=trivy_retry`.
- **cve_bin_tool**: теперь строит `cve_retry_with_count = RetryPolicy(retry_count=..., ...)` по образцу `healthcheck.py`, передаёт `retry=cve_retry_with_count`.

Тест `test_trivy_health_summary_returns_failure_payload_without_nameerror` обновлён — импортирует `RetryPolicy` и передаёт `retry=RetryPolicy(...)`.

Верификация: 1 тест пройден, `ruff check` clean, `ruff format --check` clean.

### STALE-COMMENT — `versions.env`

Строки 10–11 обновлены: упоминание «future CI lint» заменено на прямое указание job `lint-versions`.

### GITLAB-CI-SYNC — `.gitlab-ci.yml`

Добавлен job `lint-versions` в stage `lint`. Скрипт скопирован дословно из `.github/workflows/ci.yml`. Job проверяет консистентность `EL_SCA_VERSION` в `versions.env`, `pyproject.toml` и `docker-compose.yml`.

### AUDIT-ARCHIVE — архивация

9 файлов (`310-390`) перемещены в `docs/audit/archive/` через `git mv`.
Активная зона `docs/audit/` теперь содержит только `00-40` + `400-480` + `archive/`.

**Инцидент:** при первой попытке `git mv` через bash-сэндбокс произошла порча git-индекса (`fatal: index file corrupt`) из-за незавершённой транзакции и заблокированного `index.lock`. Устранено через PowerShell (`Remove-Item index.lock`) + `git read-tree HEAD`.

---

## 4. Верификация (выборка из предыдущих аудитов)

| ID | Описание | Статус |
|----|----------|--------|
| D3 | proxy_chain 4xx → `< 400` | ✅ |
| D11 | shlex.quote | ✅ |
| A_OBS-1 | `_probe_layer` → `RetryPolicy` (bc80668) | ✅ |
| A_OBS-2 | `_health_summary` → `RetryPolicy` (6202c1f) | ✅ |

---

## 5. Состояние carry-forward (после 480)

| ID | Описание | Приоритет | Действие |
|----|----------|-----------|----------|
| **PUSH** | `git push origin master` (3 коммита: 8286c62, eec9673, 6202c1f) | P2 | network required |
| **D-GITLAB** | Force-push GitLab protected branch | P2 | GitLab UI вручную |
| **B9** | NVD API key rotation | P2 | nvd.nist.gov вручную, NEVER automate |
| **DOCS-STALE** | architecture.md, operations.md — обновить CI job count, версии, API | P3 | review needed |

---

## 6. Backlog (без изменений из 470)

| Идея | Ценность | Сложность |
|------|----------|-----------|
| Scheduled automated scans (cron в compose) | Высокая | Средняя |
| License policy checks через Syft SBOM | Высокая | Средняя |
| SBOM diff между прогонами | Высокая | Средняя |
| Cross-scanner dedup (grype ∩ trivy ∩ cvebt) | Средняя | Высокая |
| Dashboard: экспорт в PDF | Средняя | Низкая |
| Diff-based alerting (Teams/Slack) | Средняя | Средняя |
| Report retention cleanup | Низкая | Низкая |
| `.editorconfig` для cross-platform consistency | Низкая | Минимальная |
