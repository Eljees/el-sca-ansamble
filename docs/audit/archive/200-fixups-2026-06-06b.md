# Fixups audit — 2026-06-06b (automated scheduled pass — cleanup & refactor)

> Automated run of the `el-sca-ansamble-checkups` scheduled task.
> Previous audit: `docs/audit/190-fixups-2026-06-06.md`
> User approval: "делай всё" (approved full plan)
>
> **Additional fixes in this pass (post-commit):**
> ruff clean (25→0 errors), python-multipart added to requirements.txt,
> el-sca-images-light.tar (605 MB) deleted from root.

---

## 0. Context and method

Automated analysis + cleanup pass; user approved all proposed changes.
Project at `D:\dev\el-sca-ansamble`, branch `feature/gui-ideas`.

---

## 1. Changes applied

### 1.1 Garbage deleted

| File | Reason |
|---|---|
| `output.cve-bin-tool.2026-06-0*.json` (×9) | Runtime scanner output leaked to project root; already in `.gitignore` |
| `tools/docker-mcp/docker_mcp.py.bak` | Stale backup file |
| `tools/docker-mcp/docker_mcp.py.bak2` | Stale backup file |
| `--exps/` (dir, 150 MB) | Dev-scratch experiments with binary + HTML reports; already `.gitignore`d |
| `-prompts/` (dir, 62 KB) | AI development prompts; already `.gitignore`d |
| `_el_cvebt_source_research/` (dir) | Research session dump; already `.gitignore`d |

### 1.2 Module relocation

`scripts/nvd_feed_import.py` → `resilient_updates/nvd_feed_import.py`

**Rationale:** the file is a Python module (functions + `if __name__ == "__main__"` guard) used
both as a library (tested via `spec_from_file_location`) and as a CLI tool (invoked from
`update_cve_bin_tool.sh`).  Placing it in `resilient_updates/` alongside the other Python modules
is consistent with the project's layout convention and allows future invocation as
`python -m resilient_updates.nvd_feed_import`.

**Files updated:**
- `tests/test_nvd_feed_import.py` — `SCRIPT_PATH` updated to `ROOT / "resilient_updates" / "nvd_feed_import.py"`
- `scripts/update_cve_bin_tool.sh` — default `feed_import` path updated to
  `resilient_updates/nvd_feed_import.py` (env override `CVE_BIN_TOOL_FEED_IMPORT` still works)

### 1.3 Documentation archived

`docs/status-and-roadmap.md` → `docs/audit/archive/status-and-roadmap.md`

The file explicitly stated `⚠️ УСТАРЕЛО (по состоянию на 2026-06-01)` and directed readers to
`docs/audit/` instead.  Moved to the archive sub-directory to remove the navigation confusion
while preserving historical content.

### 1.4 `.gitignore` — already correct

`artifacts/nvd-feeds/` was already present in `.gitignore` (added in a prior session).
No change needed.

---

## 2. Committed backlog (6 modified + 4 untracked from feature/gui-ideas work)

All modifications from the `feature/gui-ideas` branch plus the cleanup changes above were
committed as two commits:

1. **chore(cleanup):** delete garbage files, move nvd_feed_import, archive stale docs
2. **feat(gui+nvd-feed):** commit gui/orchestrator/updater changes + nvd feed scripts

Then `feature/gui-ideas` was fast-forward merged into `master`.

---

## 3. Post-cleanup metrics

| Metric | Before | After |
|---|---|---|
| Root-level garbage `output.*.json` | 9 | 0 |
| Stale `.bak` files | 2 | 0 |
| Gitignored scratch dirs in working tree | 3 (--exps, -prompts, _el_cvebt_source_research) | 0 |
| `scripts/nvd_feed_import.py` location | scripts/ (inconsistent) | resilient_updates/ (consistent) |
| `docs/status-and-roadmap.md` in docs root | Yes (explicitly stale) | Archived |
| Uncommitted files | 10 | 0 |

---

## 4. Carry-forward

| Item | Priority | Owner |
|---|---|---|
| Add smoke tests for `tools/docker-mcp/docker_mcp.py` | P3 | Automated pass |
| `make lock` — regenerate requirements.lock with hashes | P2 | User (needs PyPI) |
| NVD API key rotation | P4 | User |
| bandit B608 orphaned nosec in `cve_db_audit.py` — remove if B608 never fires | P5 | Next pass |
| Consider SBOM diffing feature | P3 | Next planning |
| Consider VEX-feed integration | P3 | Next planning |
