# Аудит 540 — 2026-06-30 (scheduled fixups)

**Дата**: 2026-06-30
**Предыдущий аудит**: [530-analysis-2026-06-30.md](530-analysis-2026-06-30.md)
**Тип**: automated scheduled-task — анализ + фикс документации
**Состояние ветки**: master, HEAD `d5448ff`

---

## 1. Краткое резюме

Этот проход выполнен автоматической задачей. Основная цель: проверить дефекты D1–D20
из `docs/audit/10-defects.md` по реальному коду и закрыть накопившееся расхождение
между файлом и кодовой базой.

**Главный вывод**: большинство дефектов, которые числились «открытыми» в документации,
были уже исправлены в предыдущих сессиях — но статус в `10-defects.md` не обновлялся.
Это устранено в этом проходе.

Новых code-дефектов не обнаружено. ruff clean, ruff format clean.

---

## 2. Верификация дефектов D1–D20 (проверено по коду 2026-06-30)

| ID | Описание | Статус | Где проверено |
|----|----------|--------|---------------|
| D1 | NVD API key в .env (Yandex sync) | ⚠️ OPEN | требует user-action |
| D2 | db_policy игнорируется | ✅ FALSE POSITIVE | cli.py + cve_db_audit.py |
| D3 | proxy_chain 4xx→«ok» | ✅ FIXED | proxy_chain.py: `< 400` + comment |
| D4 | fallback.py file:// URL на Windows | ✅ FIXED | `url2pathname(parsed.path)` |
| D5 | configs/wireguard/ отсутствует | ✅ FIXED | `configs/wireguard/wg0.conf.example` |
| D6 | Дублированный pip install cve-bin-tool | ✅ FIXED | Dockerfile.cve-bin-tool: ARG + constraint-file |
| D7 | run_scan.sh silent fallback | ✅ NOT A DEFECT | exit 3 + stderr; design intent |
| D8 | update_trivy.sh $FLAGS без массива | ⚠️ WORKAROUND | `set -- $FLAGS` + `"$@"` |
| D9 | Все Dockerfile от root | ✅ FIXED | `USER appuser` в 5/5 Dockerfile.* |
| D10 | _NON_RETRYABLE_REASONS vs retry_status_codes | ✅ ACCEPTED DESIGN | design-note + RetryPolicy.non_retryable_reasons |
| D11 | shlex_quote самописный | ✅ FIXED | `from shlex import quote as _shquote` |
| D12 | Path.stem для .tar.gz ненадёжен | ✅ FIXED | ARCHIVE_SUFFIXES tuple в `_strip_archive_suffix` |
| D13 | Windows atomic DB rename race | ✅ FIXED | `os.replace`-style, commit 2d801a0, v0.1.5 |
| D14 | Дублирующиеся _sha256/_sha512 | ✅ FIXED | `resilient_updates/_io.py` |
| D15 | _dedup_attempted_sources теряет статус | ✅ FIXED | `outcomes` list + retry_count + succeeded |
| D16 | enrichment.date_value float как дата | ✅ FIXED | `.fromtimestamp(...).isoformat()` |
| D17 | tmpfs без комментария | ✅ FIXED | comment в docker-compose.windows.override.yml |
| D18 | Dockerfile.apk-analyzer hardcoded Xmx512m | ✅ FIXED | `ARG JAVA_XMX=512m` + `"$JAVA_XMX"` |
| D19 | clean_generated.sh дубль git clean | ✅ FIXED | comment «for non-git checkouts» |
| D20 | Tracked временные каталоги | ✅ FIXED | git ls-files пуст |

---

## 3. Что сделано в этом проходе

### 3.1 `docs/audit/10-defects.md`

- Исправлен WARNING-баннер: убрана ложная фраза «D1-D18 are all closed» → точная
  формулировка (D1=OPEN, D2–D20 закрыты/WORKAROUND/accepted).
- Добавлены inline **→ FIXED / WORKAROUND / OPEN / ACCEPTED DESIGN** маркеры
  ко всем 20 записям.

### 3.2 `docs/INDEX.md`

- Обновлён указатель **latest** с `audit/350` (2026-06-17) на `audit/530` (2026-06-30).
- Убраны промежуточные строки, которые захламляли список.
- Добавлена секция **Tools** с упоминанием `tools/docker-mcp/`.

### 3.3 Коммит

```
d5448ff docs(10-defects): add FIXED/WORKAROUND/OPEN status to all D1-D20 entries;
        fix stale WARNING banner
        docs(INDEX): update latest audit pointer 350->530; trim stale entries;
        add tools/docker-mcp section
```

---

## 4. Carry-forward (после 540)

| ID | Описание | Приоритет | Действие |
|----|----------|-----------|----------|
| **PUSH** | `git push origin master` (1 коммит: d5448ff) | P2 | network required |
| **D-GITLAB** | Force-push GitLab protected branch | P2 | GitLab UI вручную + VPN |
| **B9** | NVD API key rotation | P2 | nvd.nist.gov вручную |
| **SCRIPTS-CLEANUP** | 5 one-off скриптов — кандидаты на удаление | P3 | ОК Elaria |
| **CHANGELOG-UNRELEASED** | [Unreleased] → v0.1.6 prep | P4 | при следующем релизе |

---

## 5. Метрики здоровья

| Метрика | Значение | Статус |
|---------|----------|--------|
| Тестов (Windows) | 868 | ✅ |
| Покрытие | 95% | ✅ |
| ruff check | чисто | ✅ |
| ruff format | 81 files already formatted | ✅ |
| Рабочее дерево git | чисто (после коммита) | ✅ |
| GitHub origin | 1 коммит впереди (d5448ff) | push needed |
| GitLab | рассинхронизирован | carry-forward |
| Версия | 0.1.5 | — |
| D1–D20 OPEN | 1 (D1 NVD key) | ℹ️ |
