# Аудит 570 — 2026-07-02 (scheduled checkup)

**Дата**: 2026-07-02
**Предыдущий аудит**: [560-analysis-2026-07-01.md](560-analysis-2026-07-01.md)
**Тип**: automated scheduled-task — полный чекап, изменений кода не вносилось
**Состояние ветки**: master, HEAD `8e8e416`, рабочее дерево чисто (кроме `_SCA_reports/`)

---

## 1. Краткое резюме

Чекап за 2026-07-02. С момента аудита 560 новых коммитов нет (HEAD тот же).

**Два новых находки** по сравнению с 560:

1. **`_SCA_reports/` не в `.gitignore`** — директория с реальными scan-отчётами
   (CYBERSEC-12811, community-zabbix-4.2.0) показывается как untracked в `git status`
   при каждом запуске. Требует одной строки в `.gitignore`.

2. **Flaky-тест `test_post_proxy_chain_roundtrip_all_values`** — упал при
   coverage-прогоне (WinError 10055: Windows socket buffer exhaustion), прошёл в
   изолированном запуске. Это системный шум Windows, а не баг в коде, но стоит
   пометить `@pytest.mark.flaky` или разобрать по-другому.

Все остальные carry-forward пункты (PUSH, D-GITLAB, SCRIPTS-CLEANUP, INDEX-LATEST,
30-TESTS-BANNER) — без изменений, ждут ОК или сети.

---

## 2. Метрики здоровья (срез 2026-07-02)

| Метрика | Значение | Статус |
|---------|----------|--------|
| Тестов (без coverage) | **873** | ✅ |
| Тестов (с coverage, в тяж. прогоне) | 872 passed, 1 flaky | ⚠️ |
| Тест-файлов | 43 | ✅ |
| Покрытие | 95% (gate 88%) | ✅ |
| ruff check | чисто | ✅ |
| TODO/FIXME в resilient_updates/ | 0 | ✅ |
| Рабочее дерево git | чисто (кроме `_SCA_reports/`) | ⚠️ |
| GitHub origin | **4 коммита впереди** | ⚠️ push needed |
| GitLab | рассинхронизирован | ❌ carry-forward |
| Версия | 0.1.5 | — |
| Python-модулей в resilient_updates/ | **30** (включая `__init__.py`) | ✅ |
| D1–D20 OPEN | **1** (D1 NVD key) | ℹ️ |

> **Примечание по числу коммитов**: в аудите 560 значилось «3 коммита впереди».
> После того, как был создан и закоммичен сам файл аудита 560 (`8e8e416`), счётчик
> стал 4. Это нормально.

---

## 3. Дельта с аудитом 560

### 3.1 Новые коммиты

Новых коммитов нет. HEAD = `8e8e416` (docs: audit 560 — сам файл аудита).

### 3.2 Новые расхождения, найденные в этом проходе

#### [NEW-1] `_SCA_reports/` не в `.gitignore` — NOISE в git status

`git status` всегда показывает:
```
?? _SCA_reports/
```

Директория содержит реальные scan-отчёты:
- `CYBERSEC-12811/` (6 файлов: MD + HTML, 4 per-tool HTML) — 2026-07-01
- `community-zabbix-4.2.0/` (6 файлов) — 2026-07-01

`.gitignore` содержит `artifacts/reports/*`, но `_SCA_reports/` (другая директория,
в корне репозитория) не покрыта. Отчёты — runtime-артефакты, не должны коммититься.

**Действие**: добавить `_SCA_reports/` в `.gitignore`.

#### [NEW-2] Flaky-тест при coverage-прогоне

`tests/test_dashboard.py::test_post_proxy_chain_roundtrip_all_values` упал при
длинном прогоне с coverage:

```
OSError: [WinError 10055] An operation on a socket could not be performed because
the system lacked sufficient buffer space or because a queue was full.
```

При изолированном запуске — **проходит**. `WinError 10055` = исчерпание сокетных
ресурсов Windows во время длинного тест-сета (~103 секунды). Это не баг в коде теста.

**Варианты реакции**:
a. Добавить маркер `@pytest.mark.slow` и вынести из базового prогона (текущие `slow`-тесты уже исключены в CI-команде через `-m "not integration"`, не `slow`).
b. Добавить `@pytest.mark.skipif(sys.platform == 'win32', ...)` только если это воспроизводится стабильно.
c. Мониторить: если падает >1 раза в неделю — принять меры.

**Текущая рекомендация**: мониторить (вариант c). Один инцидент — недостаточно данных.

#### [ℹ️] Carry-forward: docs/INDEX.md — устаревший указатель «latest»

`docs/INDEX.md:52` — `audit/530-analysis-2026-06-30.md` помечен как «**latest**».
Актуальный — `560-analysis-2026-07-01.md`. (Из 560, без изменений.)

**Действие**: обновить строку INDEX.md (1 мин).

#### [ℹ️] Carry-forward: docs/audit/30-tests.md — устаревший баннер

`30-tests.md:4` баннер содержит `now: `540-analysis-2026-06-30.md``.
Актуальный — `560-analysis-2026-07-01.md`. (Из 560, без изменений.)

**Действие**: обновить строку баннера (1 мин).

---

## 4. Статус дефектов D1–D20 (без изменений с 540)

| ID | Статус |
|----|--------|
| D1 | ⚠️ OPEN — NVD key rotation, manual |
| D2 | ✅ FALSE POSITIVE |
| D3–D7, D9–D20 | ✅ FIXED / ACCEPTED DESIGN / NOT A DEFECT |
| D8 | ⚠️ WORKAROUND — update_trivy.sh `set -- $FLAGS` |

---

## 5. Архитектурный снимок (без изменений с 560)

### 5.1 Scripts — SCRIPTS-CLEANUP (carry-forward)

Кандидаты на удаление (ждут ОК Elaria):

| Файл | Размер | Причина |
|------|--------|---------|
| `scripts/reproduce-cybersec-11531.sh` | 9.1 KB | one-off репро; в README строка 35 |
| `scripts/windows/reproduce-cybersec-11531.ps1` | ~8 KB | Windows-зеркало |
| `scripts/windows/commit-fixes.ps1` | ~5 KB | one-off git-хелпер |
| `scripts/windows/extract-projects.ps1` | ~2 KB | захардкоженный путь YandexDisk |
| `scripts/update_grype.sh` | 152 байта | thin shim; в README строка 16 |

**⚠️ Не трогать**: `scripts/update_trivy.sh` — entrypoint в docker-compose.yml.

### 5.2 docs/audit/ — audit-файлы

| Место | Файлов |
|-------|--------|
| `docs/audit/` (активные) | 23 (из них 5 reference 00–40, 18 нумерованных 400–570) |
| `docs/audit/archive/` | 43 |

После достижения 600-серии рекомендуется переместить 400–490 в archive.

### 5.3 scanner_diff.py — недоиспользован (SCANNER-DIFF-CLI)

`scanner_diff.py` (155 LOC покрытых, 100%) реализует сравнение scan-run'ов.
Нет `cli scan diff` entry-point. Carry-forward, ждёт ОК.

---

## 6. Carry-forward (после 570)

| ID | Описание | Приоритет | Статус |
|----|----------|-----------|--------|
| **PUSH** | `git push origin master` (4 коммита) | P2 | требует сети |
| **D-GITLAB** | Force-push GitLab через UI + VPN | P2 | ручное действие |
| **B9** | NVD API key rotation (nvd.nist.gov) | P2 | ручное действие |
| **NEW-1** | `_SCA_reports/` → `.gitignore` | P3 | ждёт ОК |
| **SCRIPTS-CLEANUP** | 5 one-off скриптов — удалить | P3 | ждёт ОК |
| **D8-FIX** | update_trivy.sh $FLAGS → массив | P3 | деферировано |
| **INDEX-LATEST** | docs/INDEX.md → 530 заменить на 560 | P4 | ⚡ 1 мин |
| **30-TESTS-BANNER** | docs/audit/30-tests.md → 540→560 | P4 | ⚡ 1 мин |
| **NEW-2** | Flaky-тест test_dashboard.py::test_post_proxy_chain_roundtrip_all_values | P4 | мониторить |
| **CHANGELOG-UNRELEASED** | [Unreleased] → v0.1.6 при следующей фиче | P5 | деферировано |
| **SCANNER-DIFF-CLI** | Добавить `cli scan diff` entry-point | P5 | ждёт ОК |

---

## 7. Поэтапный план исправлений

### Этап 1 — Быстрые (P2, требуют сети/VPN)

| # | Действие | Ресурс |
|---|----------|--------|
| 1.1 | `git push origin master` | GitHub |
| 1.2 | GitLab force-push через UI + VPN | GitLab UI |
| 1.3 | Ротация NVD API ключей | nvd.nist.gov |

### Этап 2 — Мелкие правки (P3–P4, ~10 мин, ОК нужен)

| # | Действие | Файл | Время |
|---|----------|------|-------|
| 2.1 | Добавить `_SCA_reports/` в `.gitignore` | `.gitignore` | 1 мин |
| 2.2 | Обновить docs/INDEX.md: «530» → «560» в строке «latest» | `docs/INDEX.md:52` | 1 мин |
| 2.3 | Обновить 30-tests.md баннер: «540» → «560» | `docs/audit/30-tests.md:4` | 1 мин |
| 2.4 | Git rm 5 one-off скриптов + обновить scripts/README.md | scripts/ | 15 мин |

### Этап 3 — Технический долг (P3, 30 мин, деферировано)

| # | Действие | Файл |
|---|----------|------|
| 3.1 | D8: update_trivy.sh `$FLAGS` → массив | `scripts/update_trivy.sh` |
| 3.2 | SCANNER-DIFF-CLI: добавить `cli scan diff` entry-point | `resilient_updates/cli.py` |

### Этап 4 — Архивирование и релиз (P4–P5)

| # | Действие | Когда |
|---|----------|-------|
| 4.1 | Переместить docs/audit/400–490 в archive/ | После 600-серии |
| 4.2 | Выпустить v0.1.6 | При следующей фиче |

---

## 8. План улучшений и фич

### Высокий приоритет

| Идея | Обоснование | Состояние |
|------|-------------|-----------|
| **`cli scan diff` entry-point** | `scanner_diff.py` 100% покрыт, готов к обёртке | Ждёт ОК |
| **Scheduled automated scans** (cron in compose) | Убирает ручной запуск | Нет кода |
| **License policy checks** (Syft SBOM) | Compliance; паттерн есть в `vex.py` | Нет кода |

### Средний приоритет

| Идея | Обоснование |
|------|-------------|
| **Cross-scanner dedup** (grype ∩ trivy ∩ cvebt) | `scanner_diff.py` — основа; нужна dedup-логика в отчёте |
| **Dashboard: экспорт в PDF** | `dashboard.py` уже FastAPI; endpoint несложный |
| **Diff-based alerting** (Teams/Slack) | Проактивные уведомления при новых CVE |
| **D8 full-fix** | Убрать WORKAROUND (~30 мин) |
| **Report retention cleanup** | Авто-удаление старых `_SCA_reports/` по TTL |

### Низкий приоритет

| Идея | Примечание |
|------|------------|
| `make update-grype` target | После cleanup `update_grype.sh` |
| `.editorconfig` | Cross-platform consistency |
| VEX-фильтрация Phase 2 (B4) | `vex.py` готов; нужна pipeline-интеграция |
| EPSS/KEV enrichment (B5) | `enrichment.py` готов |
| Python 3.11+ shims cleanup (15 модулей) | После `requires-python ≥3.11` |
| `provenance.py:8-11` pragma: no cover | py<3.11 блок никогда не достигается |

---

## 9. Вопросы к Elaria

1. **NEW-1: `_SCA_reports/` → `.gitignore`**: Подтверждаешь добавление строки?
   Scan-отчёты (CYBERSEC-12811, community-zabbix-4.2.0) — runtime-артефакты, не нужны в repo.

2. **SCRIPTS-CLEANUP**: Подтверждаешь удаление 5 скриптов (§5.1)?
   Давно висит — один раз сказать «да», всё удалится.

3. **Docs P4**: Разрешаешь обновить INDEX.md + 30-tests.md (≤3 мин суммарно)?
   Технически не «код», но по договорённости спрашиваю.

4. **NEW-2 flaky-тест**: Мониторим? Или сразу добавить `@pytest.mark.slow`?
   Один инцидент — рекомендую мониторить.

5. **PUSH**: Готова запушить 4 коммита в GitHub сейчас?

---

## 10. Итого

| Категория | Статус |
|-----------|--------|
| Код / тесты | ✅ Отличный (95% покрытие, 873 теста, ruff чисто) |
| CI/CD GitHub | ✅ Работает |
| CI/CD GitLab | ❌ Рассинхронизирован (carry-forward) |
| Документация | ⚠️ 2 стale-ссылки (INDEX.md, 30-tests.md) |
| Git push | ⚠️ 4 коммита не запушены |
| .gitignore | ⚠️ `_SCA_reports/` не покрыта |
| Активные дефекты | D1 (NVD key, manual) + D8 (workaround) |
| Cleanup | 📋 5 скриптов ждут ОК |
| Flaky | ⚠️ 1 тест (WinError 10055, мониторить) |

**Следующие шаги** (в порядке приоритета):
1. `git push origin master` (4 коммита)
2. Добавить `_SCA_reports/` в `.gitignore`
3. Обновить INDEX.md + 30-tests.md (≤3 мин)
4. Получить ОК на cleanup скриптов → git rm × 5

*Документ создан автоматически в ходе scheduled checkup. Изменения кода не вносились.*
