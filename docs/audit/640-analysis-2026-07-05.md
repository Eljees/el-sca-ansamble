# Аудит 640 — 2026-07-05 (scheduled checkup)

**Дата**: 2026-07-05
**Предыдущий аудит**: [630-analysis-2026-07-05.md](630-analysis-2026-07-05.md)
**Тип**: automated scheduled-task
**Состояние ветки**: master, HEAD `eab8aa7` (8 коммитов впереди origin/master)

---

## 1. Краткое резюме

Сессия прошла штатно: FUSE-обрезаний нет, стейл-lock отсутствует. Ruff ✅, AST ✅,
TODO/FIXME в коде ✅ (0). Тест-база без изменений (873 / 873).

Главные находки:
- **Закрыто** одно стейл-наблюдение: `scanner_diff.py`-DRY нарушение давно исправлено;
  из архитектурных примечаний убрать.
- **Обновлено** число коммитов впереди origin: теперь **8** (было 6 при написании 630 — в той
  же сессии добавили ещё 2 коммита).
- **Три мелких** новых P5-пункта: дублированный номер `8.` в INDEX, стейл-баннер в
  `40-tooling-docs.md`, orphan-документ `operations-guide.md`.
- **Ориентир на релиз**: [Unreleased] в CHANGELOG вырос до 8+ коммитов — можно готовить
  v0.1.6.

---

## 2. Метрики здоровья (срез 2026-07-05, вторая итерация)

| Метрика | Значение | Статус |
|---------|----------|--------|
| FUSE-обрезания | 0 | ✅ |
| `.git/index.lock` | отсутствует | ✅ |
| `ruff check` | All checks passed | ✅ |
| AST parse (все .py) | OK, нет SyntaxError | ✅ |
| TODO/FIXME/HACK в коде | 0 | ✅ |
| Тестов (Windows) | **873 / 873** | ✅ |
| Coverage | 95% (gate 88%) | ✅ |
| Git uncommitted | чисто | ✅ |
| GitHub origin | **8 коммитов впереди** | ⚠️ PUSH нужен |
| GitLab | отстаёт (VPN) | ❌ |
| D1-open | 1 (NVD key) | ℹ️ |
| Стейл pyc (не cpython-314) | **285** | ⚠️ P5 cleanup |

---

## 3. Новые находки

### 3.1 SCANNER-DIFF-DRY-STALE → **ЗАКРЫТЬ**

Архитектурное примечание (из `20-architecture.md` §1, повторяется в 620/630 §6):
> `scanner_diff.py:38 — _read_json дублирует reporting.py` (DRY-нарушение)

**Состояние на сегодня**: нарушения нет. `scanner_diff.py:36` импортирует
`first_json as _first_json` и `normalize_severity` из `._io`. В самом файле стоит
явный комментарий:

```python
# _first_json previously had a local copy here; removed in favour of the
# canonical _io.first_json import above.
```

`reporting.py:9–13` также импортирует `read_json`, `sha256_file`, `sha512_file`
из `._io`. `_io.py` является единственным источником правды. **Пункт закрыт.**

_Действие:_ удалить эту строку из раздела "Архитектурные наблюдения" будущих
carry-forward и из `docs/audit/20-architecture.md §1` при следующем обновлении этого файла.

---

### 3.2 INDEX-DOUBLE-8 (P5)

`docs/INDEX.md` §"I need to understand the project's current state" — два пункта
пронумерованы как `8.`:

```
8. `audit/610-analysis-2026-07-04.md`
8. `audit/600-analysis-2026-07-03.md`   ← дубль
9. `audit/590-analysis-2026-07-03.md`   ...
```

Ожидаемый порядок: 610 = 8, 600 = 9, 590 = 10, 580 = 11, 570 = 12, 530 = 13,
520 = 14, archive = 15, CHANGELOG = 16.

**Действие**: простая правка трёх цифр в INDEX.md (P5, ~1 мин).

---

### 3.3 40-TOOLING-STALE-BANNER (P5)

`docs/audit/40-tooling-docs.md:3-4` — WARNING-баннер ссылается на
`270-analysis-2026-06-11.md`, которого давно нет в `docs/audit/` (находится в
`archive/`). Ссылка устарела примерно на 370 аудит-единиц. Стандартная
шаблонная правка баннера.

**Действие**: обновить баннер — указать на `630-analysis-2026-07-05.md` как
"for current state".

---

### 3.4 OPERATIONS-GUIDE-ORPHAN (P5 / информационный)

`docs/operations-guide.md` (450 LOC, русскоязычный пошаговый алгоритм)
**не упоминается в `docs/INDEX.md`**. В INDEX дважды указан только
`operations.md` (305 LOC, английский). В архивных аудитах (470/490 series)
этот документ фигурировал как «нуждается в синхронизации».

Варианты:
- Добавить в INDEX.md (§"I need to scan an artefact" или §"I need to deploy")
  как основной русскоязычный гайд для операторов;
- Либо подтвердить, что документ намеренно не в INDEX (например, если он
  заменён `operations.md` + `remote-analysis.md`).

**Действие**: уточнить у Elaria (ждёт ОК).

---

### 3.5 PUSH-COUNT-UPDATE

Audit 630 фиксировал 6 коммитов впереди origin; в той же сессии добавили ещё 2
(коммиты `8f54999` и `eab8aa7`). Сейчас **8 коммитов** отделяют local master от
`origin/master`. Список:

```
eab8aa7 docs(INDEX): update latest pointer to audit 630
a26bb3b docs: audit 630 (2026-07-05); FUSE-TRUNC+LOCK-CLEANUP+3 fixes committed
8f54999 fix: STALE-30-BANNER+STALE-DEFENDER-README+LOGGING-FORMAT (A002)
f551cb9 docs: audit 620 (2026-07-04)
bba6f6c docs: audit 610 (2026-07-04)
e72cad4 docs: audit 600 (2026-07-03)
bab5bff docs: audit 590 (2026-07-03)
38c04dd fix(test): _is_windows() helper (CI-LINUX-INTERNALERROR)
```

GitLab по-прежнему далеко позади, ждёт VPN + force-push.

---

### 3.6 UNRELEASED-GROWING (информационный)

В `CHANGELOG.md [Unreleased]` накопилось значительное содержание (8+ коммитов):
`_is_windows()` хелпер, FUSE-DOCS, INDEX cleanup, AUDIT-ARCHIVE, dashboard config fix,
SCRIPTS-CLEANUP, A_OBS-2 refactor, `normalize_severity` тесты. Всё это появилось
после тега `v0.1.5` (2026-06-24). Проект технически готов к тегу **v0.1.6**.

**Действие**: при следующей активной сессии — тег v0.1.6 + push тега на GitHub + CHANGELOG.

---

## 4. Carry-forward (обновлённый)

| ID | Описание | Приоритет | Изменения |
|----|----------|-----------|----|
| **PUSH** | `git push origin master` (8 коммитов) | P2 | +2 коммита с 630 |
| **D-GITLAB** | Force-push GitLab через VPN | P2 | без изменений |
| **D1 / B9** | NVD API key rotation — ручное на nvd.nist.gov | P2 | без изменений |
| **FUSE-GUARD** | Preflight: авто-восстановление FUSE-обрезанных через WinMCP | P3 | из 630 |
| **LOCK-CLEANUP** | Preflight: авто-удаление стейл `.git/index.lock` | P3 | из 630 |
| **UNRELEASED-GROWING** | v0.1.6 tag; заполнить CHANGELOG → поднять версию | P3 | **новый** |
| **OPERATIONS-GUIDE-ORPHAN** | `operations-guide.md` не в INDEX → добавить или подтвердить намеренно | P4 (ждёт ОК) | **новый** |
| **COMPOSE-CI** | linux+offline overlay в compose-config job в ci.yml | P4 | из 610 |
| **NEW-2** | Flaky `test_post_proxy_chain_roundtrip_all_values` (WinError 10055) | P4 мониторить | из 610 |
| **INDEX-DOUBLE-8** | `docs/INDEX.md`: второй пункт `8.` → перенумеровать | P5 | **новый** |
| **40-TOOLING-STALE-BANNER** | `40-tooling-docs.md:3-4` — ссылка на `270-analysis` → обновить на 630 | P5 | **новый** |
| **PYCACHE-STALE** | 285 non-cpython-314 `.pyc` — `find . -path "*/__pycache__/*.pyc" ! -name "*.cpython-314.pyc" -delete` | P5 | +6 с 630 |
| **D8** | `update_trivy.sh` `$FLAGS` workaround (Alpine /bin/sh) | P5 | из 610 |
| **PYTHON-SHIMS** | 15 файлов с `try/except ImportError` для `datetime.UTC` | P5 при bump | из 610 |
| ~~**SCANNER-DIFF-DRY**~~ | ~~scanner_diff.py:38 дублирует reporting.py~~ | ~~архит.~~ | **ЗАКРЫТ** (см. §3.1) |

---

## 5. Поэтапный план (следующая сессия, ждёт ОК)

### Фаза 0 — уже сделано (этот файл)
- [x] `docs/audit/640-analysis-2026-07-05.md` — создан

### Фаза 1 — push (ждёт сети)
1. `git push origin master` — 8 коммитов
2. GitLab force-push через VPN

### Фаза 2 — релиз v0.1.6 (~15 мин, ждёт ОК)
3. Финализировать `[Unreleased]` в `CHANGELOG.md` → создать секцию `[0.1.6]`
4. Обновить версию в `pyproject.toml` + `versions.env` + `docker-compose.yml`
5. Тег `v0.1.6` + push тега

### Фаза 3 — мелкие P5 (~10 мин, ждёт ОК)
6. **INDEX-DOUBLE-8**: перенумеровать записи 8.–16. в `docs/INDEX.md`
7. **40-TOOLING-STALE-BANNER**: обновить баннер `40-tooling-docs.md`
8. **PYCACHE-STALE**: удалить 285 стейл-pyc командой из §2
9. **SCANNER-DIFF-DRY**: убрать из `docs/audit/20-architecture.md §1`

### Фаза 4 — CI (~15 мин, ждёт ОК)
10. **COMPOSE-CI**: добавить в `.github/workflows/ci.yml`:
    ```yaml
    - name: docker compose config (linux overlay)
      run: docker compose -f docker-compose.yml -f docker-compose.linux.override.yml config -q
    - name: docker compose config (offline overlay)
      run: docker compose -f docker-compose.yml -f docker-compose.offline.yml config -q
    ```

### Фаза 5 — preflight автоматизация (ждёт ОК, отдельная сессия)
11. **FUSE-GUARD + LOCK-CLEANUP**: добавить preflight в scheduled-task SKILL.md

---

## 6. Возможные улучшения и фичи (backlog)

| Идея | Ценность | Примечание |
|---|---|---|
| SBOM diff → CI pipeline-hook | высокая | `scanner_diff.py` готов; нужен CI webhook |
| License policy checks через Syft SBOM | высокая | ADR не написан |
| Scheduled automated scans (cron в compose) | высокая | — |
| Cross-scanner dedup (grype ∩ trivy ∩ cvebt) | средняя | снижает шум в отчётах |
| Dashboard: экспорт в PDF | средняя | `report_html.py` уже есть; нужен headless-print |
| Diff-based alerting (Teams/Slack) | средняя | — |
| EPSS/KEV freshness (ADR-0004) | средняя | ADR proposed |
| VEX feed (ADR-0003) | средняя | ADR accepted, deferred |
| Report retention cleanup (авто-удаление старых) | низкая | — |
| `.editorconfig` | низкая | — |
| `cli.py` декомпозиция (1218 LOC) | архитектурная | высокий риск регресса; обсуждение отдельно |
| `dashboard.py` декомпозиция (1135 LOC) | архитектурная | аналогично |
| Python ≥ 3.11: убрать 15 datetime.UTC shim-импортов | техдолг | при bump `requires-python = ">=3.11"` |

---

## 7. Архитектурные наблюдения (актуальные)

- `cli.py` — монолит 1218 LOC; содержит CLI-парсинг + бизнес-логику + конфиг + health-check flow
- `dashboard.py` — монолит 1135 LOC
- ~~`scanner_diff.py:38` — дублирует `reporting.py`~~ → **ЗАКРЫТО** (см. §3.1)
- 15 модулей с `try/except ImportError` для `datetime.UTC` — нужны пока CI на Python ≤ 3.11
