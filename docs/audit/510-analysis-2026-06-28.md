# Плановый анализ 510 — 2026-06-28

> Автоматический запуск scheduled task `fixups`.
> HEAD вход: `7d97ecb` (master) — «CHANGELOG-UNRELEASED + DOCS-STALE».
> Предыдущий аудит: `docs/audit/500-analysis-2026-06-27.md`.
> Режим: **анализ + исправления** — применены P3+P4-фиксы этой сессии.

---

## 1. Состояние на вход (верифицировано)

| Параметр | Значение | Статус |
|---|---|---|
| HEAD | `7d97ecb` | — |
| Версия | **0.1.5** | ✅ |
| Тег | `v0.1.5` | ✅ |
| ruff check | ✅ clean | ✅ |
| ruff format | ✅ 81 files already formatted | ✅ |
| bandit MEDIUM+ | ✅ clean (2 nosec-warnings на B108 — ожидаемо) | ✅ |
| compileall | ✅ clean | ✅ |
| 847 тестов | собраны (WindowsPath INTERNALERROR в Linux sandbox — известно) | ⚠️ known |
| Uncommitted | ✅ clean на входе | ✅ |
| origin/master | **6 коммитов впереди** | ⚠️ push нужен |

---

## 2. Верификация carry-forward из 500

| ID | Описание | Статус |
|----|----------|--------|
| **PUSH** | `git push origin master` | ❌ сеть недоступна в sandbox |
| **D-GITLAB** | Force-push GitLab | ❌ ручной |
| **B9** | NVD API key rotation | ❌ ручной (NEVER automate) |
| **DOCS-CI-SECTION** | Добавить CI/CD раздел в architecture.md | ✅ **ЗАКРЫТ** этой сессией |
| **DOCS-RETROPOLICY-NOTE** | Добавить RetryPolicy-заметку в таблицу модулей | ✅ **ЗАКРЫТ** этой сессией |
| **PROVENANCE-COVERAGE** | 88% gate boundary | ❌ P4, deferred |

---

## 3. Применённые исправления

### 3.1 DOCS-CI-SECTION — раздел CI/CD добавлен в architecture.md

**Файл:** `docs/architecture.md`, между `## Docker Compose профили` и `## Хранение данных`.

Добавлен раздел `## CI/CD` с таблицей 12 jobs в 3 стадиях:

| Стадия | Jobs |
|---|---|
| `lint` | pre-commit, lint-python, bandit, lint-shell, lint-docker, lint-yaml, lint-powershell, lint-versions |
| `build` | compose-config, docker-build (матрица 5 сервисов) |
| `test` | smoke, pytest (матрица py3.10 + py3.12, coverage ≥ 88% на 3.12) |

Также добавлено:
- Примечание о матрице `docker-build` (5 сервисов, без db-data)
- Примечание о `pytest` матрице (py3.10 без gate, py3.12 с gate 88%)
- Упоминание GitLab Cobertura coverage report

### 3.2 DOCS-RETROPOLICY-NOTE — RetryPolicy в таблице модулей

**Файл:** `docs/architecture.md`, таблица «Python-оркестратор».

| Модуль | Было | Стало |
|---|---|---|
| `cli.py` | «Точка входа, все команды CLI» | + «`_probe_layer` принимает `RetryPolicy` напрямую (A_OBS-1)» |
| `healthcheck.py` | «Проверка доступности источников» | + «`_health_summary` принимает `RetryPolicy` напрямую (A_OBS-2)» |

### 3.3 CHANGELOG [Unreleased] — дополнен

**Файл:** `CHANGELOG.md`, секция `### Changed`.

Добавлена строка о `docs/architecture.md` (CI-section + RetryPolicy-note, коммит `8358aa1`).

---

## 4. Новые находки этого прохода

### 4.1 D20 TRACKED-TEMP — закрыт (не требует действий)

`git ls-files | grep -E "exps|prompts|comands.txt|deep-research"` вернул пустой результат.
Временные артефакты не отслеживаются git. Дефект D20 из `10-defects.md` фактически закрыт.

### 4.2 D19 CLEAN-GENERATED — закрыт (комментарий уже есть)

`scripts/clean_generated.sh` содержит корректный комментарий о переносимости (non-git checkouts).
Дефект D19 также фактически закрыт предыдущим коммитом `29b6a07`.

### 4.3 Нет новых дефектов

Полный скан:
- ruff/bandit/compileall: ✅ без замечаний
- TODO/FIXME/HACK в `resilient_updates/`: ✅ 0
- Stale-комментарии в `docs/`: ✅ нет новых (проверены non-archive файлы)

---

## 5. Состояние carry-forward (после 510)

| ID | Описание | Приоритет | Действие |
|----|----------|-----------|----------|
| **PUSH** | `git push origin master` (7 коммитов) | P2 | network required |
| **D-GITLAB** | Force-push GitLab protected branch | P2 | GitLab UI вручную |
| **B9** | NVD API key rotation | P2 | nvd.nist.gov вручную |
| **PROVENANCE-COVERAGE** | 88% gate boundary — `pragma: no cover` при следующем bump | P4 | deferred |

---

## 6. Поэтапный план следующей сессии

### Этап 1 — PUSH (P2, сеть нужна)

1. `git push origin master` (7 коммитов: от `816fb45` до текущего).

### Этап 2 — Аудит 10-defects.md (P3)

2. Пометить D19 и D20 как закрытые (добавить `~~` и статус `FIXED`).

### Этап 3 — Новые находки (по мере накопления)

3. Провести точечный аудит провenance-coverage для определения `pragma: no cover` candidates.

---

## 7. Структурный анализ — что в порядке

| Компонент | Наблюдение | Вывод |
|-----------|------------|-------|
| ruff check | ✅ clean | ✅ |
| ruff format | ✅ 81 files | ✅ |
| bandit MEDIUM+ | ✅ clean | ✅ |
| compileall | ✅ clean | ✅ |
| 847 тестов | собраны (Linux WindowsPath skip — известно) | ✅ |
| architecture.md CI-раздел | добавлен | ✅ |
| architecture.md RetryPolicy | отражён | ✅ |
| CHANGELOG [Unreleased] | актуален | ✅ |
| D19, D20 | фактически закрыты | ✅ |

---

> **Статус:** 1 коммит применён (`8358aa1`). CHANGELOG обновлён. Push — после выхода сессии.
