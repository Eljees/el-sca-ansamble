# Аудит 550 — 2026-07-01 (scheduled fixups)

**Дата**: 2026-07-01  
**Предыдущий аудит**: [540-analysis-2026-06-30.md](540-analysis-2026-06-30.md)  
**Тип**: automated scheduled-task — анализ + тесты + docs  
**Состояние ветки**: master, HEAD `ac59d0b` (входной)

---

## 1. Краткое резюме

Цели прохода: проанализировать структуру проекта, выявить дефекты, недостатки тестов
и документации, предложить и применить исправления.

**Итог**: найдены и закрыты 2 реальных пробела:

1. `normalize_severity` в `_io.py` — функция не импортировалась и не тестировалась в
   `test_io.py`, несмотря на то что является публичным контрактом модуля.
   Добавлены 5 тестов.

2. `docs/audit/30-tests.md` — WARNING-баннер ссылался на аудит 310 (2026-06-13) и
   содержал устаревшие цифры (829 тестов, `healthcheck.py` = 0 тестов).
   Баннер обновлён.

ruff и тесты зелёные до и после.

---

## 2. Инструментальная верификация

### 2.1 Синтаксис

```
python3 -c "import ast, os, sys; ..."  → All .py files parse OK
ruff check .                           → All checks passed!
ruff format --check .                  → 81 files already formatted
```

### 2.2 Тесты (Linux sandbox, Python 3.10)

```
pytest tests/ -p no:cacheprovider -q --ignore=tests/pester
→ 175 passed, 1 skipped
```

Примечание: в sandbox работает 175/868 тестов. Остальные — Windows-специфичные
(WindowsPath, Pester, docker compose). Базовый счёт на Windows остаётся 868.

INTERNALERROR в pytest (NotImplementedError: cannot instantiate 'WindowsPath' on Linux) —
давно известный и задокументированный нюанс FUSE-монтирования. Не влияет на
продуктивность тестов.

### 2.3 Покрытие (Linux sandbox, 175 тестов)

| Модуль | Покрытие | Примечание |
|--------|----------|------------|
| `_io.py` | 41% | до фикса `normalize_severity` не импортировалась |
| `monitor.py` | 38% | тесты есть, покрытие низкое из-за FUSE/WindowsPath |
| `dashboard.py` | 7% | FastAPI endpoint-тесты требуют event-loop |
| `orchestrator.py` | 13% | subprocess/threading-heavy |
| `nvd_feed_import.py` | 13% | сетевые зависимости |
| `route_plan.py` | 17% | docker env-dependent |
| TOTAL | 50% | CI gate 75% достигается на Windows (868 тестов) |

---

## 3. Что сделано

### 3.1 `tests/test_io.py` — добавлены тесты `normalize_severity`

```python
def test_normalize_severity_uppercases()
def test_normalize_severity_none_returns_unknown()
def test_normalize_severity_empty_string_returns_unknown()
def test_normalize_severity_zero_returns_unknown()
def test_normalize_severity_non_string_input()
```

Импорт `normalize_severity` добавлен в секцию импортов файла.
Все 15 тестов в `test_io.py` зелёные.

### 3.2 `docs/audit/30-tests.md` — обновлён WARNING-баннер

- Цифра тестов: 829 → **868**
- Ссылка на актуальный аудит: 310 → **540**
- Факт: `healthcheck.py` теперь **100%**, не 0%
- Факт: пробел `normalize_severity` закрыт 2026-07-01

---

## 4. Что НЕ менялось (обоснованно)

| Область | Вывод |
|---------|-------|
| `monitor.py` 38% | Тесты уже полные (12 функций в `test_monitor.py`). Низкое число от FUSE-эффекта, не пробела. |
| `orchestrator.py` 13% | subprocess/Queue/threading — требует docker. Известно из предыдущих аудитов. |
| `nvd_feed_import.py` 13% | Сетевые/файловые зависимости. Не меняется в sandbox. |
| `route_plan.py` 17% | docker env. Аналогично. |
| SCRIPTS-CLEANUP | Carry-forward P3 из 540. Не удалять без OK Elaria. |
| CHANGELOG [Unreleased] | Carry-forward P4 из 540. Нет новых фич для v0.1.6. |
| D1 (NVD key) | Требует user-action. |

---

## 5. Carry-forward (после 550)

| ID | Описание | Приоритет |
|----|----------|-----------|
| **PUSH** | `git push origin master` (3 коммита: ac59d0b + этот) | P2 |
| **D-GITLAB** | Force-push GitLab + VPN | P2 |
| **B9** | NVD API key rotation | P2 |
| **SCRIPTS-CLEANUP** | 5 one-off скриптов — кандидаты на удаление (OK Elaria) | P3 |
| **CHANGELOG-UNRELEASED** | v0.1.6 prep при следующем релизе | P4 |

---

## 6. Метрики здоровья

| Метрика | Значение | Статус |
|---------|----------|--------|
| Тестов (Windows) | 868 | ✅ |
| Тестов (Linux sandbox) | 175 pass / 1 skip | ✅ |
| ruff check | чисто | ✅ |
| ruff format | 81 files formatted | ✅ |
| Python compileall | OK | ✅ |
| Версия | 0.1.5 | — |
| D1–D20 OPEN | 1 (D1 NVD key) | ℹ️ |
| GitHub | push needed (3 коммита) | ⚠️ |
| GitLab | рассинхронизирован | carry-forward |
