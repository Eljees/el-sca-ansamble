# Аудит 650 — 2026-07-06 (cleanup + monitor snapshots)

**Дата**: 2026-07-06
**Тип**: operator-requested cleanup / implementation pass
**Состояние ветки до sync-pass**: `master`, локально впереди `origin/master` на 9 коммитов; впереди `gitlab/master` на 39 коммитов.

## 1. Что сделано

- Добавлен `AGENTS.md`: проектные правила, команды проверки, политика cleanup и git sync.
- `docs/INDEX.md`: добавлен `operations-guide.md`, обновлён latest pointer на этот аудит.
- `docs/audit/20-architecture.md`: убран закрытый carry-forward `SCANNER-DIFF-DRY`; баннер обновлён на 873 tests / audit 640+.
- `docs/audit/40-tooling-docs.md`: stale pointer `270-analysis` заменён на актуальный audit line.
- `.github/workflows/ci.yml`: `compose-config` теперь проверяет base, windows, linux и offline overlays.
- GUI/monitor: `/api/monitor` теперь отдаёт `latest_run`; dashboard показывает последний snapshot/checkpoint.
- Long-run safety: периодический checkpoint dashboard-run теперь вызывает `snapshot_artifacts()`, а не только пишет `checkpoint.json`.
- Runtime cleanup: live `artifacts/` очищен после лёгкого snapshot.

## 2. Cleanup evidence

Перед очисткой сохранён snapshot:

```text
artifacts/runs/PRE-CLEAN-20260706-20260706-180448
```

Размер live runtime:

```text
artifacts/: 776.7 MB -> 11.7 MB
*.pyc:      406 -> 0
```

Оставлены намеренно:

- `.env` — локальная конфигурация/секреты, не трогать автоматикой;
- `configs/feed_sources.runtime.yaml` — runtime override proxy-chain;
- `artifacts/runs/` — сохранённые run snapshots.

## 3. Проверки

Локальный targeted gate:

```text
python -m pytest tests/test_monitor.py tests/test_orchestrator.py tests/test_dashboard.py -q
126 passed

python -m ruff check resilient_updates/orchestrator.py resilient_updates/monitor.py resilient_updates/dashboard.py tests/test_monitor.py tests/test_orchestrator.py tests/test_dashboard.py
All checks passed
```

Полный gate должен быть выполнен после завершения всех правок:

```text
python -m ruff check .
python -m ruff format --check resilient_updates tests scripts tools
python -m compileall -q resilient_updates tests
python -m pytest -q
```

## 4. Оставшийся план

1. Полная валидация всех тестов/линтеров.
2. При необходимости — smoke dashboard через FastAPI TestClient или локальный uvicorn.
3. Отдельный sync-pass:
   - `git fetch --all --prune`;
   - проверить `master...origin/master` и `master...gitlab/master`;
   - отдельно решить push/tag для GitHub и reconcile GitLab.

## 5. Feature backlog

- Dashboard: отдельная вкладка `Run Monitor` с фильтром контейнеров, live tail по выбранному сервису, кнопками cancel/retry/resume.
- Resume: валидировать stage outputs перед skip, например SBOM/report/provenance existence + checksum.
- Artifacts: retention policy для `artifacts/runs/*` и явная команда `el-sca prune-runs --keep N`.
- Reporting: PDF export из HTML report.
- Architecture: постепенно разнести `dashboard.py` на routes/ui/helpers и `cli.py` на subcommand modules.
