# Плановый анализ 500 — 2026-06-27

> Автоматический запуск scheduled task `el-sca-ansamble-checkups`.
> HEAD вход: `509dcd1` (master) — «docs: audit 490».
> Предыдущий аудит: `docs/audit/490-analysis-2026-06-26.md`.
> Режим: **анализ + исправления** — применены P3-фиксы этой сессии.

---

## 1. Состояние на вход (верифицировано)

| Параметр | Значение | Статус |
|---|---|---|
| HEAD | `509dcd1` | — |
| Версия | **0.1.5** | ✅ |
| Тег | `v0.1.5` | ✅ |
| Тестов | **847 collected** (Linux sandbox, 36 Windows-only skipped) | ✅ |
| ruff check | ✅ clean | ✅ |
| Uncommitted | ✅ clean на входе | ✅ |
| origin/master | **5 коммитов впереди** | ⚠️ push нужен |

---

## 2. Верификация carry-forward из 490

| ID | Описание | Статус |
|----|----------|--------|
| **PUSH** | `git push origin master` | ❌ сеть недоступна в sandbox |
| **D-GITLAB** | Force-push GitLab | ❌ ручной |
| **B9** | NVD API key rotation | ❌ ручной (NEVER automate) |
| **CHANGELOG-UNRELEASED** | Добавить A_OBS-2 + GITLAB-CI-SYNC в [Unreleased] | ✅ **ЗАКРЫТ** этой сессией |
| **DOCS-STALE** | architecture.md — счётчики и профили | ✅ **ЗАКРЫТ** (частично — count+profiles) |
| **PROVENANCE-COVERAGE** | 88% gate boundary | ❌ P4, деferred |

---

## 3. Применённые исправления

### 3.1 CHANGELOG-UNRELEASED — заполнен `[Unreleased]`

**Файл:** `CHANGELOG.md`, строки 7–18.

Добавлены две секции:
- `### Refactored` — `_health_summary` принимает `retry: RetryPolicy` (A_OBS-2, `6202c1f`).
- `### Changed` — обновление комментария `versions.env`; job `lint-versions` в `.gitlab-ci.yml` (GITLAB-CI-SYNC, `6202c1f`).

---

### 3.2 DOCS-STALE / compose-count — architecture.md обновлён

**Файл:** `docs/architecture.md`, строка 164.

| Было | Стало |
|------|-------|
| `2026-06-09` | `2026-06-27` |
| `21 сервис` | `25 сервисов` |
| `15 профилей` | `17 профилей` |

---

### 3.3 DOCS-STALE / missing-profiles — два профиля добавлены в таблицу

**Файл:** `docs/architecture.md`, таблица профилей.

Добавлены две строки:

| Профиль | Описание |
|---------|----------|
| `route` | route-doctor — зондирование egress и выбор маршрута per-tool (ADR-0007 P1.5) |
| `volinit` | volume-init — предварительная инициализация named volumes с нужными правами |

---

## 4. Новые находки этого прохода

### 4.1 DOCS-STALE / CI-section — в architecture.md нет раздела CI

**Наблюдение:** В `docs/architecture.md` нет раздела о CI/CD pipeline. В `490` упоминалось
«нет `lint-powershell`, `lint-versions`» — теперь их нет во всём файле, потому что CI-раздел
отсутствует вообще.

CI-pipeline (`.github/workflows/ci.yml`) включает 8 jobs:
`pre-commit`, `lint-python`, `lint-shell`, `sast`, `lint-docker`, `lint-yaml`,
`lint-powershell`, `lint-versions`.

GitLab (`.gitlab-ci.yml`) — паритет: `pre-commit`, `lint-python`, `lint-shell`, `sast`,
`lint-docker`, `lint-yaml`, `lint-powershell`, `lint-versions`.

**Приоритет:** P3 — документационный долг.

**Действие:** Добавить раздел `## CI/CD` в `architecture.md` с таблицей jobs.

---

### 4.2 DOCS-STALE / architecture-RetryPolicy — не отражён рефакторинг A_OBS-1/A_OBS-2

**Наблюдение:** В разделе «Python-оркестратор» таблица модулей не упоминает, что
`healthcheck.py` и `cli.py` теперь принимают `RetryPolicy` напрямую.
`_retry.py` уже есть в таблице модулей — это хорошо.

**Приоритет:** P4 — незначительное.
**Действие:** При следующем значимом обновлении архитектуры добавить строку в описание
`healthcheck.py`/`cli.py` о `RetryPolicy`-параметрах.

---

## 5. Состояние carry-forward (после 500)

| ID | Описание | Приоритет | Действие |
|----|----------|-----------|----------|
| **PUSH** | `git push origin master` (6 коммитов после этого) | P2 | network required |
| **D-GITLAB** | Force-push GitLab protected branch | P2 | GitLab UI вручную |
| **B9** | NVD API key rotation | P2 | nvd.nist.gov вручную |
| **DOCS-CI-SECTION** | Добавить CI/CD раздел в architecture.md | P3 | ~20 строк |
| **DOCS-RETROPOLIY-NOTE** | Добавить RetryPolicy-заметку в healthcheck.py/cli.py описание | P4 | 2 строки |
| **PROVENANCE-COVERAGE** | 88% gate boundary — рассмотреть `pragma: no cover` | P4 | при следующем bump gate |

---

## 6. Поэтапный план исправлений (следующая сессия)

### Этап 1 — CI-section в architecture.md (P3)

1. Добавить раздел `## CI/CD` после раздела «Docker Compose профили».
2. Таблица из 8 jobs с кратким описанием каждого.
3. Примечание: GitLab CI зеркалит GitHub Actions.

*~25 строк, 1 файл, 10 минут.*

### Этап 2 — PUSH (P2, сеть нужна)

4. `git push origin master` после накопленных коммитов.

---

## 7. Структурный анализ — что в порядке

| Компонент | Наблюдение | Вывод |
|-----------|------------|-------|
| ruff check | ✅ clean | ✅ |
| Тесты | 847 pass (36 skipped — Windows-only) | ✅ |
| CHANGELOG [Unreleased] | Заполнен | ✅ |
| architecture.md compose-count | 25 сервисов / 17 профилей | ✅ |
| architecture.md profiles table | route + volinit добавлены | ✅ |
| TODO/FIXME в коде | 0 | ✅ |
| Uncommitted на выходе | 3 файла (CHANGELOG, architecture, этот doc) | — |

---

> **Статус:** Изменения применены. Коммит и push — после выхода сессии.
