# Плановый анализ 2026-06-23b

> Автоматический запуск scheduled task `fixups`.
> Базовая линия: коммит `816fb45` (HEAD, master).
> Предыдущий аудит: `docs/audit/440-analysis-2026-06-23.md`.
> Режим: **анализ + правки** — все P3 carry-forward из 440 применены.

---

## 1. Состояние на вход

| Параметр | Значение | Статус |
|---|---|---|
| HEAD | `816fb45` | без изменений на входе |
| Версия (pyproject.toml) | **v0.1.4** | ✅ |
| Версия (versions.env) | **v0.1.4** | ✅ |
| Тестов (последний прогон) | **868 passed**, 37 skipped | ✅ |
| Покрытие | **95.18%** (gate 88%) | ✅ |
| ruff | ✅ clean | ✅ |
| `origin/master` | **2 commits ahead** | 🔴 push нужен |

---

## 2. Выполненные правки

### O3 — CHANGELOG: запись D_NEW-1 — ЗАКРЫТ

Добавлена секция `### Changed` в `## [Unreleased]`:

```
- `docker-compose.yml`: все 12 образов `elariaphd/el-sca-*` переведены с захардкоженного
  `:0.1.1` на `${EL_SCA_VERSION:-0.1.4}` — деплой теперь подхватывает актуальную
  версию из `versions.env`. (`816fb45`)
```

### D_NEW-3 — `requirements.lock` удалён — ЗАКРЫТ

`git rm requirements.lock` — файл полностью удалён из репозитория.
Комментарий в `ci.yml` про «stale requirements.lock placeholder» также упрощён.

### O4 — Баннеры `00-overview.md` + `10-defects.md` — ЗАКРЫТ

Конкретная ссылка `(now: 410-analysis-2026-06-21.md)` заменена на общую
`` `NNN-analysis-*.md` `` — баннер больше не устаревает после каждого аудита.

### D_NEW-2 — CI `lint-versions` job — ЗАКРЫТ

Добавлен новый job между `lint-yaml` и `compose-config`:

```yaml
lint-versions:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - name: Check version consistency
      run: |
        # сверяет versions.env / pyproject.toml / docker-compose.yml fallback
        ...
```

Теперь CI содержит **12 jobs** (было 11).

### O7 — Архивация `docs/audit/240–300` — ЗАКРЫТ

7 файлов перемещены в `docs/audit/archive/` через `git mv`:
`240-analysis-2026-06-08.md`, `250-fixups-2026-06-09.md`,
`260-fixups-2026-06-11.md`, `270-analysis-2026-06-11.md`,
`280-analysis-2026-06-12.md`, `290-analysis-2026-06-12.md`,
`300-fixups-2026-06-12.md`.

Активная папка: 24 файла → **17 файлов** (310–450 + ref-docs).

### O5 — Audit-файлы 430+440 добавлены в git — ЗАКРЫТ

`git add docs/audit/430-analysis-2026-06-22.md docs/audit/440-analysis-2026-06-23.md`

---

## 3. Состояние после правок

| Параметр | Значение |
|---|---|
| HEAD (после коммита) | `TBD` |
| ruff | ✅ (изменения не затрагивают .py файлы) |
| Тестов | **868** (без изменений — правки только в docs/ci/changelog) |
| CI jobs | **12** (было 11, добавлен lint-versions) |
| `requirements.lock` | ❌ удалён (git rm) |
| `docs/audit/` активных файлов | **17** (было 24) |

---

## 4. Открытые carry-forward (после 450)

| ID | Описание | Приоритет | Действие |
|----|----------|-----------|----------|
| **PUSH** | `git push origin master` (теперь 3 коммита) | P1 | При наличии сети |
| **D-GITLAB** | Force-push GitLab protected branch | P2 | GitLab UI |
| **B9** | NVD API key rotation | P2 | nvd.nist.gov вручную, NEVER automate |
| **A_OBS-1** | `_probe_layer` → принять `RetryPolicy` напрямую | P3 backlog | отдельный PR |
| **REL-0.1.5** | v0.1.5 тег + CHANGELOG update + push | P3 | После PUSH |

Все O3, D_NEW-3, O4, D_NEW-2, O7, O5 — **закрыты**.
