# Fixups 2026-06-09 (scheduled pass)

> Автоматический scheduled pass (`fixups`).
> Предыдущий аудит: `docs/audit/240-analysis-2026-06-08.md`

---

## Базовая линия

| Параметр | Значение |
|---|---|
| Ветка | `master` |
| Коммит на старте | `0ec3536` |
| Тесты | 617 passed |
| ruff | All checks passed |
| Открытых дефектов | 1 (D13 Windows atomic rename — P3) |

---

## Выполненные действия

### 1. Документация (план 1.1 и 1.2 из 240-analysis)

**`docs/INDEX.md`** — обновлена ссылка «latest analysis»:
- Строка 50: `220-analysis-2026-06-07.md` → `240-analysis-2026-06-08.md` (latest)
- Добавлена ссылка на `230-fixups-2026-06-08.md` (предыдущий фикс-пасс)
- Нумерация скорректирована (7 → 8 для archive/)

**`docs/architecture.md`** — профиль `db-bundle` добавлен в таблицу:
- Заголовок таблицы: `13 профилей` → `15 профилей` (верная цифра: default+update+scan+extract+report+offline+airgap+test-failover+apk+win+osv+proxy+vpn+dashboard+**db-bundle**)
- Дата в заголовке: 2026-06-04 → 2026-06-09
- Новая строка: `| \`db-bundle\` | Экспорт/импорт DB-бандлов (оффлайн передача баз) | db-exporter, db-importer |`

### 2. Тестовое покрытие enrichment.py: 88% → ~95% (+8 тестов)

Добавлены тесты в `tests/test_enrichment.py` (итого 23 теста, было 15):

| Тест | Что покрывает |
|---|---|
| `test_safe_exists_returns_false_on_oserror` | `_safe_exists` except OSError (PermissionError в контейнере) |
| `test_load_epss_scores_empty_after_metadata_header_returns_empty` | `if not first: return out` — файл только с #metadata строкой |
| `test_load_epss_scores_missing_required_columns_returns_empty` | `if cve_idx is None or epss_idx is None: return out` |
| `test_load_epss_scores_oserror_on_open_is_skipped` | `except OSError: continue` в `load_epss_scores` |
| `test_load_epss_scores_percentile_malformed_stored_as_empty` | `except ValueError` на percentile → `""` (строка сохраняется) |
| `test_load_kev_set_invalid_json_is_skipped` | `except json.JSONDecodeError: continue` |
| `test_load_kev_set_oserror_on_read_is_skipped` | `except OSError: continue` в `load_kev_set` |
| `test_candidate_roots_respects_env_vars` | `EL_SCA_ENRICHMENT_ROOT` / `CVE_BIN_TOOL_DB_ROOT` в `_candidate_roots()` |

### 3. Тестовое покрытие orchestrator.py: 79% → ~90% (+17 тестов)

Добавлены тесты в `tests/test_orchestrator.py` (итого 31 тест, было 14):

| Тест | Что покрывает |
|---|---|
| `test_parse_progress_*` (×4) | `parse_progress()` — size-ratio, %-only, no-match, clamp |
| `test_job_finalize_*` (×2) | `Job.finalize()` — статус done и error |
| `test_job_end_stage_ok_false_marks_error` | `end_stage(key, False)` → `"error"` |
| `test_subscribe_running_job_receives_updates` | `Job.subscribe()` к живому job + pub-sub поток |
| `test_attach_log_writes_lines_to_file` | `Job.attach_log()` + `_write_log_locked` + `_close_log_locked` |
| `test_run_stream_file_not_found_returns_127` | `except FileNotFoundError` в `_run_stream` → rc=127 |
| `test_extract_produced_output_*` (×4) | `_extract_produced_output()` — manifest pass, count>0, нет файла, пустой JSON |
| `test_archive_reports_copies_files_to_history` | `_archive_reports()` с реальными файлами |
| `test_archive_reports_noop_when_no_final_dir` | ранний выход при отсутствии final/ |
| `test_sse_stream_emits_keepalive_before_job_finishes` | `except Empty: yield ": keep-alive"` в `sse_stream` |

---

## Финальный статус

| Параметр | До | После |
|---|---|---|
| Тесты | 617 passed | **642 passed** (+25) |
| ruff | ✅ clean | ✅ clean |
| Покрытие enrichment.py | ~88% | ~95% (est.) |
| Покрытие orchestrator.py | ~79% | ~90% (est.) |
| Документация | INDEX.md устарел; db-bundle не в таблице | ✅ Исправлено |

---

## Carry-forward (без изменений)

| # | Действие | Приоритет | Ответственный |
|---|---|---|---|
| 0.1 | `git push gitlab master` | P1 | Elaria |
| 0.2 | `make lock` → `requirements.txt` с хешами | P2 | Elaria |
| 0.3 | Ротация NVD API ключей | P3 | Elaria |
| D13 | `cve_db_audit._activate` Windows atomic rename | P3 | next pass |

---

[[session-2026-06-08-fixups]] [[project-state]]
