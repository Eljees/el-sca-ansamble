# Fixups audit — 2026-06-01d (fourth automated pass)

> Automated analysis on top of
> `docs/audit/130-fixups-2026-06-01c.md` (same session).

---

## 0. State at start of pass

| Check | Result |
|---|---|
| Last audit | `130-fixups-2026-06-01c.md` |
| `ruff check .` | **0 errors** ✅ |
| `pytest -q` | **260 passed** ✅ |

---

## 1. Fixes applied this pass

### 1.1 `tests/test_reporting_helpers.py` — 38 unit tests for `reporting.py` helpers (P3, **FIXED**)

`reporting.py` had 538 lines with a single end-to-end test.  All 15+
pure helper functions were exercised only transitively.  Added
`tests/test_reporting_helpers.py` (38 tests) covering:

| Function | Tests |
|---|---|
| `_normalize_severity` | 7 parametrized (case fold, falsy, None, 0) |
| `_syft_count` | 5 (artifacts, components, empty, non-dict, unknown key) |
| `_grype_findings` | 6 (typical, empty, null, id-fallback, unknown, cvss score) |
| `_trivy_findings` | 6 (typical, empty, null, empty-vulns, unknown, nvd score) |
| `_cve_bin_tool_findings` | 7 (list, dict findings/results keys, empty, null, unknown, non-dicts) |
| `_dedup_findings` | 3 (same tuple, different tool, different version) |
| `_markdown_table` | 4 (empty, renders rows, sort order, EPSS/KEV columns) |

All 38 tests pass.

### 1.2 `@pytest.mark.smoke` added to both new test files (P3, **FIXED**)

`test_vex.py` and `test_reporting_helpers.py` had no `smoke` marks, so
the fast CI gate (`pytest -m smoke`, < 30 s) would never catch regressions
in those modules.  Added one `@pytest.mark.smoke` marker to each file on
the cheapest representative test:

- `test_vex.py::test_vex_dir_uses_cache_dir`
- `test_reporting_helpers.py::test_normalize_severity` (parametrized — all 7 variants run)

---

## 2. State at end of pass

| Metric | Before | After |
|---|---|---|
| Test count | 260 | **298** (+38) |
| `reporting.py` direct tests | 1 | **39** (1 existing + 38 new helpers) |
| `vex.py` smoke coverage | ❌ | ✅ |
| `reporting.py` smoke coverage | ❌ | ✅ |

---

## 3. Files changed this pass

| File | Change |
|---|---|
| `tests/test_reporting_helpers.py` | **New** — 38 unit tests for `reporting.py` helpers |
| `tests/test_vex.py` | Add `@pytest.mark.smoke` to `test_vex_dir_uses_cache_dir` |
| `tests/test_reporting_helpers.py` | Add `@pytest.mark.smoke` to `test_normalize_severity` |
| `docs/audit/140-fixups-2026-06-01d.md` | This file |

---

## 4. Cumulative fix count for 2026-06-01 session (all four passes)

| Fix | File(s) |
|---|---|
| 18 vex tests | `tests/test_vex.py` |
| 38 reporting helper tests | `tests/test_reporting_helpers.py` |
| Smoke marks on new files | `test_vex.py`, `test_reporting_helpers.py` |
| Makefile `--maxfail=1` | `Makefile` |
| `scanner_diff` dedup | `resilient_updates/scanner_diff.py` |
| pre-commit ruff bump | `.pre-commit-config.yaml` |
| CHANGELOG update | `CHANGELOG.md` |
| Audit docs | `docs/audit/110`–`140` |

---

## 5. Remaining carry-forward (host action only)

| Sev | Item |
|---|---|
| P0/sec | Rotate NVD API keys |
| P1 | `make lock` → real `requirements.lock` with hashes |
| P1 | Delete `.git/*.lock*` stale files |
| P3 | Delete `.git\info\refs (2)` |

---

**See also:** [130-fixups-2026-06-01c.md](130-fixups-2026-06-01c.md)
