# Audit 300 — Fixups 2026-06-12 (session 3)

Third automated fixup pass for 2026-06-12.  Previous pass: `290-analysis-2026-06-12.md`.

## Scope

Commits `bc96337` → `5d4b640` (on top of `a0bfca5` by previous sub-pass).

## Fixes applied

### D-300-1 — `pyproject.toml` version drift (`bc96337`)

`version = "0.1.0"` was stale; `versions.env` and CHANGELOG both said `0.1.1`.
Fixed: `version = "0.1.1"`.

### D-300-2 — CI stale audit link (`bc96337`)

`.github/workflows/ci.yml` comment referenced `docs/audit/30-tests.md section 4` which
no longer describes the test strategy.  Fixed to `docs/audit/ for current test strategy`.

### D-300-3 — CHANGELOG comparison links (`bc96337`)

Footer links were `example.invalid` placeholders.  Replaced with real GitHub URLs for
`v0.1.0`, `v0.1.1`, and HEAD comparisons.

### D-300-4 — `docker-compose.yml` `volume-init` entry added to CHANGELOG (`bc96337`)

The `pre-mkdir` fix for 7 `artifacts/` subdirectories (committed in `b36347a`) had no
CHANGELOG entry.  Added to `[Unreleased]`.

### D-300-5 — `CONTRIBUTING.md` stale facts (`a0bfca5`)

Three bugs:
- Coverage gate text said `--cov-fail-under=75`; should be `88`.
- Test command examples omitted `smoke` / `integration` markers entirely.
- "Adding a new test" guidance had `smoke` and `integration` roles reversed.

All three fixed; correct marker semantics documented (`smoke` = fast sanity,
`integration` = Docker/compose/network).

### D-300-6 — `scripts/README.md` missing 9 entries (`a0bfca5`)

Nine scripts existed in `scripts/` with no README entry:
`update-db.sh`, `scan.sh`, `export_images.sh`, `import_images.sh`, `pack_light.sh`,
`deploy_light.sh`, `export_db_image.sh`, `import_db_image.sh`,
`reproduce-cybersec-11531.sh`.  All nine added.

### D-300-7 — `tests/test_orchestrator.py` uncommitted tests (`a0bfca5`)

96 lines (10 tests) existed in the working tree but had never been committed.
All 48 tests in the file pass; included in commit.

### D-300-8 — `docs/architecture.md` — 8 missing modules + 5 missing CLI commands (`5d4b640`)

Module table was missing `orchestrator.py`, `dashboard.py`, `route_plan.py`,
`update_doctor.py`, `nvd_feed_import.py`, `scan.py`, `vex.py`, `run_layout.py`.
CLI table was dated 2026-05-25 and missing `scan`, `dashboard`, `update-doctor`,
`route-plan`, `archive-run`.  Both tables updated; date bumped to 2026-06-12.

### D-300-9 — Stale test count in audit banners (`300-fixups-2026-06-12.md`)

Old WARNING banners in `00/10/20/30-overview.md` said "712 tests"; actual count is 722.
Fixed in all four files.

## State after this pass

- Tests: **722 passed** (`pytest -q -m "not integration"`)
- `ruff check .` — clean
- GitHub HEAD: `5d4b640`
- GitLab: still blocked on protected-branch force-push (Elaria only)
- Open P3: D13 — Windows atomic rename in `cve_db_audit._activate`
