# Fixups audit — 2026-05-27 (automated pass)

> Automated analysis run on top of `docs/audit/60-fixups-2026-05-26.md`.
> Picks up the deferred-sprint issues still open after the previous pass
> and applies the safe, non-breaking subset automatically.

---

## 1. Issues reviewed this pass

| ID | Severity | Category | Summary | Status |
|---|---|---|---|---|
| §18 | P2 | Config | `JAVA_TOOL_OPTIONS=-Xmx512m` hardcoded in `Dockerfile.apk-analyzer` | ✅ Fixed |
| §15 | P2 | Bug | `_dedup_attempted_sources` last-wins loses per-URL retry counts | ✅ Fixed |
| smoke | P1 | Tests | `@pytest.mark.smoke` defined but no test used it; `pytest -m smoke` would collect nothing | ✅ Fixed |
| §12 | P2 | Bug | `_strip_archive_suffix` falls through to `Path.stem` for `.tar.gz` | ✅ Already fixed — false carry-over |
| §13 | P2 | Bug | `atomic_publish._activate` race condition on Windows NTFS | ⏳ Deferred |
| §19 | P3 | Tooling | `clean_generated.sh` duplicate `git clean -fdX` | ✅ Already fixed — false carry-over |
| NEW-3 | P1 | Tooling | `requirements.lock` is a placeholder | ⏳ User action required |
| NEW-5 | P0 | Security | NVD API key rotation + `git rm --cached` large files | ⏳ User action required |
| NEW-6 | P1 | Tests | `pytest --cov` baseline unknown; `--cov-fail-under=75` untested | ⏳ User action required |

---

## 2. Changes applied this pass

### 2.1  `Dockerfile.apk-analyzer` — configurable JVM heap (§18)

**Problem.** `JAVA_TOOL_OPTIONS="-Xmx512m"` was a hardcoded `ENV` directive.
Memory-constrained or high-throughput deployments (many parallel APK scans)
had no way to adjust the JVM heap without rebuilding from a modified
Dockerfile.

**Fix.** Added `ARG JAVA_XMX=512m` before the `ENV` block; `JAVA_TOOL_OPTIONS`
now reads `"-Xmx${JAVA_XMX}"`.  Default behaviour is unchanged (512 MB).

Override at build time:
```sh
docker compose build --build-arg JAVA_XMX=1g apk-analyzer
```
Or via `.env` / `versions.env`:
```
JAVA_XMX=1g
```
`docker-compose.yml` `apk-analyzer.build.args` wired accordingly:
```yaml
args:
  JAVA_XMX: ${JAVA_XMX:-512m}
```

---

### 2.2  `resilient_updates/cli.py` — accumulate retry counts (§15)

**Problem.** `_dedup_attempted_sources` used `last-wins` dict semantics
(`seen[item.source.name] = item.source.to_dict()`).  When the retry loop in
`attempt_sources` produced multiple `AttemptResult` entries for the same URL
(each retry appends one), the dedup function discarded all but the last.
The output JSON reported only the final attempt, giving no visibility into
how many retries were made and what failed.

**Fix.** The function now:
- Preserves insertion order (first encounter wins for source metadata).
- Accumulates `retry_count` — total attempts against that source name.
- Sets `succeeded = True` if *any* attempt succeeded.
- Appends each attempt's `{success, reason, message, status_code}` to an
  `outcomes` list.

Sample output for a source that failed twice then succeeded:
```json
{
  "name": "primary-mirror",
  "url": "https://example.com/grype-db.tar.gz",
  "retry_count": 3,
  "succeeded": true,
  "outcomes": [
    {"success": false, "reason": "http_5xx", "message": "http status 503", "status_code": 503},
    {"success": false, "reason": "http_5xx", "message": "http status 503", "status_code": 503},
    {"success": true,  "reason": null,        "message": "ok",              "status_code": 200}
  ]
}
```

Callers of `_dedup_attempted_sources` (three call sites in `cli.py`) are
unchanged — they pass `list[AttemptResult]` and receive `list[dict]` as
before.  The extra keys are additive and do not break existing downstream
consumers of `artifacts/provenance/*.json`.

---

### 2.3  Tests — `@pytest.mark.smoke` finally populated

**Problem.** `pytest.ini` declared a `smoke` marker and the CI plan
(§30-tests §3) called for a fast `pytest -m smoke` gate, but no test was
actually tagged.  Running `pytest -m smoke` collected zero tests and always
passed vacuously.

**Fix.** Three fast, zero-network unit tests marked `@pytest.mark.smoke`:

| File | Test |
|---|---|
| `tests/test_config_validation.py` | `test_config_validation_catches_duplicates_and_invalid_values` |
| `tests/test_retry_policy.py` | `test_defaults_match_legacy_grype_listing_fetch` |
| `tests/test_logging_setup.py` | `test_setup_logging_text_format_emits_one_line` |

These cover config loading + validation, the retry-policy dataclass defaults,
and the structured logging setup — three critical-path modules that, if broken,
would silently corrupt every scan run.

`pytest.ini` marker description updated from "heavy-ish integration tests" to
the correct meaning: "fast critical-path sanity tests; run with `-m smoke` as a
quick gate before the full suite".

---

### 2.4  False carry-overs confirmed closed

**§12** (`_strip_archive_suffix` / `Path.stem`): The audit noted this as
deferred, but the code already handles it correctly.  `ARCHIVE_SUFFIXES` in
`extractor.py` lines 17–29 lists `.tar.gz`, `.tar.bz2`, `.tar.xz`, etc.
The function only falls through to `Path(name).stem` for formats *not* in
that tuple (e.g., unknown extensions), which is the correct fallback.

**§19** (`clean_generated.sh` duplicate `git clean`): The current file
contains no `git clean` call at all.  This issue was resolved in a prior
commit.

---

## 3. Remaining user-action items (carry-over)

These cannot be automated — they require credentials or interactive git operations.

### 3.1  NVD API key rotation (NEW-5) — required before pushing

`.env.local` contains `NVD_API_KEY` values that transited through Yandex
Disk (cloud storage) and should be considered exposed.

1. Request two new API keys at <https://nvd.nist.gov/developers/request-an-api-key>.
2. Revoke the old keys via the same portal.
3. Put the new values in `.env.local`.
4. Remove tracked research artefacts:
   ```
   git rm --cached "deep-research-report(4).md"
   git rm --cached "Исследование контейнеризации, обновления баз и fallback-механизмов для cve-bin-tool, Trivy, Grype и .docx"
   git commit -m "chore: untrack research artefacts"
   ```

### 3.2  Real lockfile (NEW-3)

`requirements.lock` is a human-written placeholder, not a pip-compile output.
CI `pip install -r requirements.lock` installs latest rather than pinned
versions.

```sh
pip install pip-tools
pip-compile --strip-extras --generate-hashes -o requirements.lock requirements.in
```

Commit and push the result.

### 3.3  Coverage baseline (NEW-6)

Run `pytest -q --cov=resilient_updates --cov-report=term-missing` on the
Windows host before the next push to `master`.  If coverage is below 75 %,
temporarily lower `--cov-fail-under` in `.github/workflows/ci.yml` and file a
task to reach 75 %.

---

## 4. Deferred to next sprint

| ID | Estimate | Note |
|---|---|---|
| §13 | ~1 h | `atomic_publish` Windows NTFS race — use temp dir on same volume + `shutil.move` for truly atomic swap |

---

*Generated by automated fixups pass — 2026-05-27*
